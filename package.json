{
  "devDependencies": {
    "lerna": "^3.13.1",
    "prettier": "^1.16.4"
  },
  "name": "iotv",
  "private": true,
  "scripts": {
    "build": "yarn run lerna run build",
    "generate": "yarn run lerna run generate",
    "test": "yarn run lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
